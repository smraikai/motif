name: Checks
on: [push, pull_request, workflow_dispatch]
permissions:
  contents: read
jobs:
  macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: ./macos/check.sh
      - run: xcrun swiftc -swift-version 5 -O macos/Sources/*.swift -o /tmp/Motif
